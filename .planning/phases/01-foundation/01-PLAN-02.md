---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/db/sqlite.ts
  - src/core/db/lance.ts
  - src/core/db/sqlite.test.ts
  - src/core/db/lance.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-03]
must_haves:
  truths:
    - "SQLite database initializes at ~/.claude-code-memory/metadata.db with correct schema"
    - "First DB open stores embedding_model_id and schema_version in index_metadata table"
    - "Matching model ID on subsequent open passes without error"
    - "Mismatched model ID throws a clear error before any write"
    - "LanceDB connects and opens/creates chunks table at ~/.claude-code-memory/lancedb/"
  artifacts:
    - path: "src/core/db/sqlite.ts"
      provides: "SQLite metadata DB client with schema init and model fingerprint"
      exports: ["openMetadataDb", "assertModelMatch"]
    - path: "src/core/db/lance.ts"
      provides: "LanceDB vector DB client with table init"
      exports: ["openChunksTable", "connectLanceDb"]
  key_links:
    - from: "src/core/db/sqlite.ts"
      to: "better-sqlite3"
      via: "Database constructor"
      pattern: "new Database"
    - from: "src/core/db/sqlite.ts"
      to: "index_metadata table"
      via: "SQL CREATE TABLE and SELECT/INSERT"
      pattern: "index_metadata"
    - from: "src/core/db/lance.ts"
      to: "@lancedb/lancedb"
      via: "connect function"
      pattern: "connect.*lancedb"
---

<objective>
Initialize SQLite and LanceDB clients with schema versioning and model fingerprint tracking.

Purpose: The metadata store (SQLite) and vector store (LanceDB) are the two persistence layers every later phase depends on. Schema versioning from day one prevents painful migrations later.
Output: Working DB clients with schema init, model mismatch detection, and passing tests.
</objective>

<execution_context>
@/Users/rod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/types.ts:
```typescript
export interface Config {
  vaultPath: string;
  indexPath: string;
  embeddingProvider: 'openai' | 'ollama';
  openaiModel: string;
  batchSize: number;
  concurrency: number;
}
```

From src/config.ts:
```typescript
export function loadConfig(overrides?: Partial<Config>): Config;
export function assertPathSafety(indexPath: string): void;
```

From src/logger.ts:
```typescript
export const logger: {
  info: (msg: string, ...args: unknown[]) => void;
  warn: (msg: string, ...args: unknown[]) => void;
  error: (msg: string, ...args: unknown[]) => void;
  debug: (msg: string, ...args: unknown[]) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite metadata DB with schema versioning</name>
  <files>src/core/db/sqlite.ts, src/core/db/sqlite.test.ts</files>
  <read_first>
    src/types.ts
    src/config.ts
    src/logger.ts
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    SQLite tests (src/core/db/sqlite.test.ts):
    - openMetadataDb(tmpDir) creates metadata.db file in tmpDir
    - openMetadataDb sets WAL journal mode and busy_timeout 5000
    - Created DB has index_metadata table (SELECT name FROM sqlite_master WHERE type='table' AND name='index_metadata')
    - Created DB has files table with columns: path, content_hash, indexed_at, chunk_count, staleness_score
    - assertModelMatch(db, 'openai:text-embedding-3-small') on fresh DB inserts the model ID into index_metadata
    - assertModelMatch(db, 'openai:text-embedding-3-small') on fresh DB inserts schema_version = '1'
    - assertModelMatch(db, 'openai:text-embedding-3-small') called twice with same ID does NOT throw
    - assertModelMatch(db, 'ollama:nomic-embed-text') after storing 'openai:text-embedding-3-small' throws Error containing 'mismatch'
    - The mismatch error message contains both the stored and current model IDs
  </behavior>
  <action>
1. Create directory `src/core/db/` if it does not exist.

2. Write tests FIRST in `src/core/db/sqlite.test.ts`:
   - Use vitest `beforeEach`/`afterEach` with `fs.mkdtempSync` to create a temp directory for each test
   - Clean up temp dir in afterEach
   - Test all behaviors listed above

3. Implement `src/core/db/sqlite.ts`:
   - Import `Database` from 'better-sqlite3', `join` from 'path', `logger` from '../../logger'
   - `openMetadataDb(indexPath: string): Database.Database`:
     - Create new Database at `join(indexPath, 'metadata.db')`
     - Set `pragma journal_mode = WAL`
     - Set `pragma busy_timeout = 5000`
     - Execute CREATE TABLE IF NOT EXISTS for `index_metadata` (key TEXT PRIMARY KEY, value TEXT NOT NULL)
     - Execute CREATE TABLE IF NOT EXISTS for `files` (path TEXT PRIMARY KEY, content_hash TEXT NOT NULL, indexed_at INTEGER NOT NULL, chunk_count INTEGER NOT NULL DEFAULT 0, staleness_score REAL DEFAULT 0.0)
     - Return the db instance
   - `assertModelMatch(db: Database.Database, providerModelId: string): void`:
     - SELECT value FROM index_metadata WHERE key = 'embedding_model_id'
     - If no row: INSERT embedding_model_id and schema_version = '1', return
     - If row exists and value matches providerModelId: return (no-op)
     - If row exists and value differs: throw Error with message containing both model IDs and "mismatch"
     - Use logger.warn before throwing to log the mismatch

4. Run tests: `npx vitest run src/core/db/sqlite.test.ts`
  </action>
  <verify>
    <automated>cd /Users/rod/Projects/claude-code-memory && npx vitest run src/core/db/sqlite.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/db/sqlite.ts` exports `openMetadataDb` and `assertModelMatch`
    - `src/core/db/sqlite.ts` contains `CREATE TABLE IF NOT EXISTS index_metadata`
    - `src/core/db/sqlite.ts` contains `CREATE TABLE IF NOT EXISTS files`
    - `src/core/db/sqlite.ts` contains `journal_mode = WAL`
    - `src/core/db/sqlite.ts` contains `busy_timeout = 5000`
    - `src/core/db/sqlite.ts` contains `embedding_model_id` string
    - `src/core/db/sqlite.ts` contains `schema_version` string
    - `src/core/db/sqlite.ts` uses `logger` from '../../logger' (not console.log)
    - `src/core/db/sqlite.test.ts` has at least 7 test cases
    - `npx vitest run src/core/db/sqlite.test.ts` exits 0
  </acceptance_criteria>
  <done>SQLite metadata DB initializes with correct schema, stores model fingerprint on first open, rejects mismatched models on subsequent opens. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LanceDB vector store client</name>
  <files>src/core/db/lance.ts, src/core/db/lance.test.ts</files>
  <read_first>
    src/types.ts
    src/core/db/sqlite.ts
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    LanceDB tests (src/core/db/lance.test.ts):
    - connectLanceDb(tmpDir) returns a LanceDB connection object
    - openChunksTable on a fresh DB creates the 'chunks' table
    - openChunksTable on an existing DB opens the existing 'chunks' table without error
    - The chunks table schema includes vector field (Float32 array), text field, source_path field, heading_path field, chunk_hash field, indexed_at field, embedding_model_id field
  </behavior>
  <action>
1. Write tests FIRST in `src/core/db/lance.test.ts`:
   - Use `fs.mkdtempSync` for temp directory per test
   - Test connection creation and table creation/opening
   - Verify schema by inserting a sample row and reading it back

2. Implement `src/core/db/lance.ts`:
   - Import `connect` from '@lancedb/lancedb'
   - `connectLanceDb(dbPath: string)`: calls `connect(dbPath)` and returns the connection
   - `openChunksTable(connection, vectorDimension: number = 1536)`:
     - Try to open existing table named 'chunks'
     - If table doesn't exist, create it with schema:
       - `id`: string (primary key — format: `{source_path}#{chunk_index}`)
       - `vector`: Float32 array of length `vectorDimension`
       - `text`: string (chunk content)
       - `source_path`: string (relative path from vault root)
       - `heading_path`: string (e.g., "# Title > ## Section > ### Subsection")
       - `chunk_hash`: string (xxhash of content)
       - `indexed_at`: number (unix ms timestamp)
       - `embedding_model_id`: string (e.g., "openai:text-embedding-3-small")
     - Return the table handle

   Note: LanceDB v0.27.x uses `db.createTable(name, data)` with an initial data array to infer schema, or `db.createEmptyTable(name, schema)` with an Arrow schema. Use the data-based approach with a single sample row that gets immediately deleted, OR use the Arrow schema approach if the API supports it cleanly. Check `@lancedb/lancedb` docs via the codebase or Context7 if needed. The critical thing is the table exists with the correct column types.

3. Run tests: `npx vitest run src/core/db/lance.test.ts`
  </action>
  <verify>
    <automated>cd /Users/rod/Projects/claude-code-memory && npx vitest run src/core/db/lance.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/db/lance.ts` exports `connectLanceDb` and `openChunksTable`
    - `src/core/db/lance.ts` imports from '@lancedb/lancedb'
    - `src/core/db/lance.ts` contains string 'chunks' (table name)
    - `src/core/db/lance.ts` contains `vector` field reference
    - `src/core/db/lance.ts` contains `embedding_model_id` field reference
    - `src/core/db/lance.test.ts` has at least 4 test cases
    - `npx vitest run src/core/db/lance.test.ts` exits 0
  </acceptance_criteria>
  <done>LanceDB connects and initializes chunks table with full schema including vector, metadata fields, and embedding_model_id. All tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/core/db/` — all DB tests pass
- `npx tsc --noEmit` — no type errors
- `grep -r "console.log" src/core/db/ --include="*.ts" --exclude="*.test.*"` — no stdout leaks
</verification>

<success_criteria>
- SQLite initializes with index_metadata and files tables
- Model fingerprint stored on first open, validated on subsequent opens
- Model mismatch throws clear error with both model IDs
- LanceDB connects and creates/opens chunks table with correct schema
- All tests pass via `npx vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
